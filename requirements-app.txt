# ============================================================================
# requirements-app.txt -- the LINE OA ("หมอทุเรียน") backend service.
#
# This is a SEPARATE install list from requirements.txt (the training
# pipeline): it keeps the serving Docker image lean by dropping training-only
# packages (scikit-learn, imagehash, seaborn, grad-cam, jupyter, ...) while
# keeping the exact inference-path deps (torch/torchvision/timm/pillow/yaml)
# so app/ml/predictor.py can import src/models.py and src/data.py unchanged.
#
# INSTALL (CPU is enough for single-image serving latency):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements-app.txt
# ============================================================================

# --- inference path (must match requirements.txt pins) --------------------
torch>=2.2,<2.7
torchvision>=0.17,<0.22
timm>=1.0.7
numpy>=1.26,<2.2
pillow>=10.2
PyYAML>=6.0

# --- web service --------------------------------------------------------------
fastapi>=0.111
uvicorn[standard]>=0.29
line-bot-sdk>=3.11
pydantic-settings>=2.2
python-dotenv>=1.0
python-multipart>=0.0.9

# --- storage -----------------------------------------------------------------
sqlalchemy>=2.0

# --- dev / test ----------------------------------------------------------------
pytest>=8.0
httpx>=0.27              # FastAPI TestClient transport
